%%
override clutter_offscreen_effect_get_target noargs
static PyObject *
_wrap_clutter_offscreen_effect_get_target (PyGObject *self)
{
        CoglHandle hnd = COGL_INVALID_HANDLE;

#if CLUTTER_CHECK_VERSION (1, 4, 0)
        hnd = clutter_offscreen_effect_get_target (CLUTTER_OFFSCREEN_EFFECT (self->obj));
#endif
        return pycogl_handle_from_handle (hnd);
}
%%
